---
import Base from '../../layouts/Base.astro';

// ── Estudio: datos verificados (motor Optimiza, julio 2026) ──
const D = {
  revisadas: 32,
  legibles: 29,
  excluidas: 3,
  indice: 57,
  pilares: [
    { k: 'Visibilidad', v: 68, note: 'SEO y datos estructurados' },
    { k: 'Conversión', v: 64, note: 'CTA, formularios, WhatsApp' },
    { k: 'Velocidad', v: 57, note: 'PageSpeed móvil (Lighthouse)' },
    { k: 'Automatización', v: 32, note: 'Medición y retargeting' },
  ],
  sinPixel: 94,
  conPixel: 2,
  sinAnalytics: 48,
  sinWhatsApp: 55,
  reprEvelocidad: 44,
  grados: [
    { g: 'A', label: '≥ 80 · Excelente', n: 0, cls: 'gA' },
    { g: 'B', label: '70–79 · Bien', n: 2, cls: 'gB' },
    { g: 'C', label: '55–69 · Regular', n: 18, cls: 'gC' },
    { g: 'D', label: '40–54 · Deficiente', n: 11, cls: 'gD' },
    { g: 'E', label: '< 40 · Crítico', n: 1, cls: 'gE' },
  ],
  // Tesis transversal (dental + estéticas + médicos)
  saludTotal: 104,
  saludSinPixel: 94,
};
const totalGrados = D.grados.reduce((s, x) => s + x.n, 0);

const publishDate = '2026-07-16';
const dateHuman = new Date(publishDate + 'T12:00:00').toLocaleDateString('es-CR', { year: 'numeric', month: 'long', day: 'numeric' });

const title = 'Consultorios médicos en Costa Rica: el 94% no mide su web';
const description = 'Analizamos 32 consultorios y especialidades médicas del GAM: el 94% no tiene Meta Pixel y casi la mitad no usa Google Analytics. Datos, metodología y hallazgos.';
const canonical = new URL(Astro.url.pathname, Astro.site).href;
const ogImage = new URL('/og/estudio-consultorios-medicos.png', Astro.site).href;

const faqs = [
  {
    q: '¿Qué tan buenas son las páginas web de los consultorios médicos en Costa Rica?',
    a: 'Son deficientes en promedio. En un estudio de 32 consultorios y especialidades médicas del Gran Área Metropolitana, el Índice Optimiza promedio fue de 57 sobre 100 (grado C). Ninguno alcanzó el grado A. El punto más débil es la automatización y medición: el 94% no puede medir ni recuperar a los pacientes que visitan su web.',
  },
  {
    q: '¿Cuál es el problema más común en las webs de los médicos especialistas?',
    a: 'La falta de medición y la lentitud. El 94% de los consultorios analizados no tiene el píxel de Meta, casi la mitad no usa Google Analytics y el 44% reprueba en velocidad de carga móvil. Además, más de la mitad no ofrece un WhatsApp directo para agendar.',
  },
  {
    q: '¿Es igual en todas las especialidades médicas?',
    a: 'El patrón se repite en ginecología, pediatría, ortopedia, oftalmología, gastroenterología, urología, salud mental y más. Cambia el detalle, pero el vacío de medición es transversal: los consultorios dependen del boca a boca y de las referencias, y descuidan la web como canal medible.',
  },
  {
    q: '¿Qué es el Índice Optimiza?',
    a: 'Es una calificación de 0 a 100 que resume la salud de un sitio web en cuatro pilares: velocidad (30%), visibilidad o SEO (30%), conversión (20%) y automatización o medición (20%). Se calcula con la API de PageSpeed Insights de Google más un análisis del HTML de la página.',
  },
  {
    q: '¿Cómo se realizó el estudio de consultorios médicos?',
    a: 'Se analizaron 32 consultorios y centros de especialidades médicas con dominio propio del Gran Área Metropolitana en julio de 2026, usando el motor de diagnóstico de Optimiza (PageSpeed Insights API y análisis de HTML). De ellos, 29 arrojaron datos completos; 3 no pudieron leerse por bloqueo de bots o por depender de JavaScript.',
  },
];

const articleLd = {
  '@context': 'https://schema.org',
  '@type': 'Report',
  headline: 'El 94% de los consultorios médicos de Costa Rica no puede medir su web',
  description,
  image: ogImage,
  datePublished: publishDate,
  dateModified: publishDate,
  inLanguage: 'es-CR',
  author: { '@type': 'Organization', name: 'Optimiza', url: 'https://optimizahq.com' },
  publisher: {
    '@type': 'Organization',
    name: 'Optimiza',
    url: 'https://optimizahq.com',
    logo: { '@type': 'ImageObject', url: 'https://optimizahq.com/favicon.svg' },
  },
  mainEntityOfPage: canonical,
  keywords: ['consultorio médico Costa Rica', 'sitio web médico', 'marketing médico', 'Meta Pixel', 'especialidades médicas', 'Gran Área Metropolitana'],
  about: { '@type': 'Thing', name: 'Páginas web de consultorios y especialidades médicas en Costa Rica' },
};

const datasetLd = {
  '@context': 'https://schema.org',
  '@type': 'Dataset',
  name: 'Diagnóstico web de consultorios médicos del GAM 2026',
  description: 'Puntajes del Índice Optimiza (velocidad, visibilidad, conversión y automatización) de 29 consultorios y especialidades médicas del Gran Área Metropolitana de Costa Rica.',
  creator: { '@type': 'Organization', name: 'Optimiza', url: 'https://optimizahq.com' },
  temporalCoverage: '2026-07',
  spatialCoverage: { '@type': 'Place', name: 'Gran Área Metropolitana, Costa Rica' },
  measurementTechnique: 'PageSpeed Insights API (Lighthouse) y análisis de HTML',
  variableMeasured: ['Velocidad', 'Visibilidad (SEO)', 'Conversión', 'Automatización', 'Índice Optimiza'],
  isAccessibleForFree: true,
  url: canonical,
};

const faqLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};

const plus = 'M12 5v14M5 12h14';
---
<Base title={title} description={description} active="analisis">
  <Fragment slot="head">
    <meta property="og:image" content={ogImage} />
    <meta property="article:published_time" content={publishDate} />
    <meta property="article:author" content="Optimiza" />
    <script type="application/ld+json" set:html={JSON.stringify(articleLd)} />
    <script type="application/ld+json" set:html={JSON.stringify(datasetLd)} />
    <script type="application/ld+json" set:html={JSON.stringify(faqLd)} />
  </Fragment>

  <article class="section">
    <div class="wrap art">
      <a class="back" href="/analisis"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12 19-7-7 7-7"></path><path d="M19 12H5"></path></svg>Análisis y teardowns</a>

      <header class="art-head reveal">
        <span class="tag res">Investigación propia</span>
        <h1>El 94% de los consultorios médicos de Costa Rica no puede medir su web</h1>
        <p class="art-meta">{dateHuman} · Estudio de {D.legibles} consultorios del Gran Área Metropolitana · Lectura 6 min</p>
      </header>

      <div class="tldr reveal">
        <p class="tldr-k">En resumen</p>
        <p>Analizamos <strong>{D.legibles} consultorios y especialidades médicas</strong> del Gran Área Metropolitana. El Índice Optimiza promedio fue de <strong>{D.indice}/100 (grado C)</strong> y ninguno alcanzó el grado A. El patrón del sector se repite: <strong>el {D.sinPixel}% no tiene el píxel de Meta</strong> (solo {D.conPixel} de {D.revisadas}), casi la mitad no usa Analytics y el {D.reprEvelocidad}% carga lento. Consultorios que viven de las referencias, invisibles a su propia medición.</p>
      </div>

      <div class="stats reveal">
        <div class="stat"><div class="sn">{D.sinPixel}<small>%</small></div><div class="sl">sin píxel de Meta (no pueden hacer retargeting)</div></div>
        <div class="stat"><div class="sn">{D.sinAnalytics}<small>%</small></div><div class="sl">sin Google Analytics</div></div>
        <div class="stat"><div class="sn">{D.sinWhatsApp}<small>%</small></div><div class="sl">sin WhatsApp directo en la web</div></div>
        <div class="stat"><div class="sn">{D.indice}<small>/100</small></div><div class="sl">Índice Optimiza promedio (grado C)</div></div>
      </div>

      <div class="prose">
        <h2>El médico que cura, pero no diagnostica su web</h2>
        <p>Los consultorios médicos comparten un descuido: su web es una tarjeta de presentación estática, no una herramienta de captación. Cargan lento, dependen del boca a boca y —sobre todo— no miden nada de lo que pasa en ellas.</p>

        <div class="bars">
          {D.pilares.map((p) => (
            <div class="bar">
              <div class="bar-top"><span>{p.k}<em>{p.note}</em></span><span class="bar-v">{p.v}<small>/100</small></span></div>
              <div class="track"><span style={`width:${p.v}%;background:${p.v < 40 ? '#E24B4A' : p.v < 60 ? '#EF9F27' : 'var(--violet)'}`}></span></div>
            </div>
          ))}
        </div>

        <p>La <strong>velocidad (57) y la automatización (32)</strong> son las más débiles. Muchos sitios corren sobre plantillas antiguas y pesadas, y casi ninguno tiene instalada una sola herramienta de medición.</p>

        <h2>El punto ciego: nadie mide</h2>
        <p>De {D.revisadas} consultorios, <strong>solo {D.conPixel} tienen el píxel de Meta</strong> y menos de la mitad usa Google Analytics. Sin medición, un consultorio no sabe:</p>
        <ul>
          <li>Cuántas personas <strong>buscan su especialidad</strong> y terminan agendando con otro.</li>
          <li>Si su inversión en publicidad <strong>trae pacientes reales</strong> o solo clics.</li>
          <li>A quién <strong>volver a contactar</strong> de los que visitaron la web y no reservaron.</li>
        </ul>
        <blockquote>Un especialista puede diagnosticar a un paciente en minutos, pero su web lleva años sin un solo diagnóstico.</blockquote>

        <h2>El patrón de todo el sector salud</h2>
        <p>Este es el tercer estudio de nuestra serie de salud. Sumado al de <a href="/analisis/estudio-webs-clinicas-dentales-costa-rica-2026">clínicas dentales</a> y al de <a href="/analisis/estudio-webs-clinicas-esteticas-costa-rica-2026">medicina estética</a>, el hallazgo es rotundo:</p>
        <div class="thesis">
          <div class="thesis-n">{D.saludSinPixel}<small>%</small></div>
          <div class="thesis-t">de las <strong>{D.saludTotal} clínicas de salud del GAM</strong> que analizamos —dentales, estéticas y consultorios médicos— <strong>no puede medir su web.</strong></div>
        </div>
        <p>No es un descuido puntual: es el <strong>estándar de toda la categoría.</strong> Para cualquier consultorio, ahí está la ventaja competitiva que casi nadie está tomando.</p>

        <h2>Distribución por calificación</h2>
        <p>Ninguno de los {D.revisadas} consultorios revisados alcanzó el grado A. El grueso se acumula entre regular y deficiente.</p>
        <div class="dist">
          {D.grados.map((g) => (
            <div class="drow">
              <div class="dg">{g.g}<em>{g.label}</em></div>
              <div class="dtrack"><span class={g.cls} style={`width:${totalGrados ? (g.n / totalGrados) * 100 : 0}%`}></span></div>
              <div class="dn">{g.n}</div>
            </div>
          ))}
        </div>

        <h2>Metodología</h2>
        <p>Transparencia total sobre cómo se hizo, porque un dato sin método es una opinión.</p>
        <ul>
          <li><strong>Muestra:</strong> {D.revisadas} consultorios y centros de especialidades médicas con dominio propio del Gran Área Metropolitana (ginecología, pediatría, cardiología, ortopedia, oftalmología, gastroenterología, otorrino, urología, salud mental, dermatología clínica, nutrición y medicina general), seleccionados de búsquedas públicas por especialidad. No es un muestreo aleatorio.</li>
          <li><strong>Herramienta:</strong> el motor de diagnóstico de Optimiza, que combina la API de PageSpeed Insights de Google (Lighthouse) con un análisis del HTML de cada página.</li>
          <li><strong>Métrica:</strong> el Índice Optimiza (0–100) pondera velocidad (30%), visibilidad/SEO (30%), conversión (20%) y automatización/medición (20%). Es una métrica compuesta propia, no una calificación oficial de Google.</li>
          <li><strong>Momento:</strong> medición única (snapshot) de julio de 2026.</li>
          <li><strong>Exclusiones:</strong> {D.excluidas} de los {D.revisadas} no pudieron leerse (bloqueo de bots o dependencia de JavaScript) y se excluyeron de los promedios.</li>
          <li><strong>Ética:</strong> no publicamos el nombre de ningún consultorio ni sus puntajes individuales. Este estudio es agregado, para retratar el estado de la categoría, no para exponer a ningún negocio.</li>
        </ul>

        <h2>Preguntas frecuentes</h2>
      </div>

      <div class="faq">
        {faqs.map((f) => (
          <div class="qa">
            <button class="q" aria-expanded="false">{f.q}<span class="ic"><svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d={plus} stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg></span></button>
            <div class="a"><p>{f.a}</p></div>
          </div>
        ))}
      </div>

      <div class="art-cta">
        <p>¿Tu consultorio vive del boca a boca? Mirá qué tan medible es tu web, gratis.</p>
        <a class="btn btn-lime" href="/#top">Analizar la web de mi consultorio gratis</a>
      </div>
    </div>
  </article>
</Base>

<style>
  .art { max-width: 760px; }
  .back { display: inline-flex; align-items: center; gap: 7px; font-family: var(--mono); font-size: 12.5px; letter-spacing: 0.04em; color: var(--muted); margin-bottom: 32px; transition: color .16s; }
  .back:hover { color: var(--violet-ink); }
  .art-head { margin-bottom: 30px; }
  .tag { font-family: var(--mono); font-size: 10.5px; letter-spacing: 0.12em; text-transform: uppercase; font-weight: 600; padding: 5px 10px; border-radius: 7px; display: inline-block; }
  .tag.res { color: var(--cyan-ink); background: rgba(14,150,176,0.12); }
  .art-head h1 { font-size: clamp(29px, 4.4vw, 44px); margin: 18px 0 0; }
  .art-meta { font-family: var(--mono); font-size: 12.5px; color: var(--faint); margin-top: 16px; }

  .tldr { background: var(--surface); border: 1px solid var(--line); border-left: 3px solid var(--violet); border-radius: 14px; padding: 22px 24px; margin-bottom: 28px; box-shadow: var(--card-shadow); }
  .tldr-k { font-family: var(--mono); font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--violet-ink); margin: 0 0 10px; font-weight: 700; }
  .tldr p:last-child { margin: 0; color: var(--text); font-size: 16.5px; line-height: 1.7; }

  .stats { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 40px; }
  @media (max-width: 640px) { .stats { grid-template-columns: 1fr 1fr; } }
  .stat { background: #0B0A12; border-radius: 14px; padding: 20px 18px; }
  .stat .sn { font-family: var(--mono); font-size: 34px; font-weight: 700; color: var(--lime); letter-spacing: -0.03em; line-height: 1; }
  .stat .sn small { font-size: 16px; color: #9A95B3; }
  .stat .sl { font-size: 12.5px; color: #9A95B3; margin-top: 10px; line-height: 1.4; }

  .prose :global(h2) { font-family: var(--display); font-weight: 600; letter-spacing: -0.02em; font-size: clamp(22px, 3vw, 29px); margin: 44px 0 14px; }
  .prose :global(p) { color: var(--muted); font-size: 17px; line-height: 1.75; margin-bottom: 18px; }
  .prose :global(ul) { margin: 0 0 20px; padding-left: 22px; color: var(--muted); }
  .prose :global(li) { margin-bottom: 10px; line-height: 1.65; font-size: 16.5px; }
  .prose :global(strong) { color: var(--text); font-weight: 600; }
  .prose :global(a) { color: var(--violet-ink); text-decoration: underline; text-underline-offset: 2px; }
  .prose :global(blockquote) { border-left: 3px solid var(--violet); padding: 4px 0 4px 20px; margin: 24px 0; color: var(--text); font-style: italic; font-size: 18px; line-height: 1.6; }

  .bars { display: flex; flex-direction: column; gap: 16px; margin: 26px 0 8px; }
  .bar-top { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 7px; }
  .bar-top span { font-size: 14.5px; color: var(--text); font-weight: 500; }
  .bar-top em { display: block; font-style: normal; font-size: 12px; color: var(--faint); font-weight: 400; margin-top: 2px; }
  .bar-v { font-family: var(--mono); font-weight: 700; color: var(--text); }
  .bar-v small { color: var(--faint); font-size: 12px; font-weight: 400; }
  .track { height: 9px; background: rgba(23,20,31,0.08); border-radius: 99px; overflow: hidden; }
  :global([data-theme="dark"]) .track { background: rgba(255,255,255,0.09); }
  .track span { display: block; height: 100%; border-radius: 99px; }

  .thesis { display: flex; align-items: center; gap: 22px; background: #0B0A12; border-radius: 16px; padding: 26px 28px; margin: 22px 0; }
  .thesis-n { font-family: var(--mono); font-size: 62px; font-weight: 700; color: var(--lime); letter-spacing: -0.04em; line-height: 1; flex: none; }
  .thesis-n small { font-size: 26px; }
  .thesis-t { color: #C9C4DE; font-size: 16px; line-height: 1.55; }
  .thesis-t strong { color: #fff; }
  @media (max-width: 520px) { .thesis { flex-direction: column; align-items: flex-start; gap: 10px; } }

  .dist { display: flex; flex-direction: column; gap: 10px; margin: 22px 0; }
  .drow { display: grid; grid-template-columns: 150px 1fr 28px; align-items: center; gap: 14px; }
  .dg { font-family: var(--mono); font-size: 15px; font-weight: 700; color: var(--text); }
  .dg em { display: block; font-style: normal; font-size: 11px; color: var(--faint); font-weight: 400; margin-top: 2px; letter-spacing: 0.02em; }
  .dtrack { height: 22px; background: rgba(23,20,31,0.05); border-radius: 7px; overflow: hidden; }
  :global([data-theme="dark"]) .dtrack { background: rgba(255,255,255,0.06); }
  .dtrack span { display: block; height: 100%; border-radius: 7px; min-width: 2px; }
  .dtrack .gA { background: #21B36B; } .dtrack .gB { background: #7FCf3F; }
  .dtrack .gC { background: #EF9F27; } .dtrack .gD { background: #E8743B; } .dtrack .gE { background: #E24B4A; }
  .dn { font-family: var(--mono); font-weight: 700; color: var(--text); text-align: right; }

  .faq { margin: 8px 0 0; }
  .qa { border-bottom: 1px solid var(--line); }
  .qa .q { width: 100%; background: none; border: none; text-align: left; display: flex; justify-content: space-between; align-items: center; gap: 16px; padding: 18px 0; font-family: var(--display); font-weight: 600; font-size: 17px; color: var(--text); cursor: pointer; }
  .qa .ic { flex: none; color: var(--violet-ink); transition: transform .2s; }
  .qa.open .ic { transform: rotate(45deg); }
  .qa .a { max-height: 0; overflow: hidden; transition: max-height .28s ease; }
  .qa .a p { color: var(--muted); font-size: 16px; line-height: 1.7; margin: 0 0 18px; }

  .art-cta { margin-top: 44px; padding-top: 32px; border-top: 1px solid var(--line); text-align: center; }
  .art-cta p { color: var(--text); font-size: 18px; font-weight: 500; margin-bottom: 18px; }
</style>
